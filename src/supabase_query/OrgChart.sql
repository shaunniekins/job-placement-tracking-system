CREATE TABLE "Organization" (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  position VARCHAR(255) NULL,
  name VARCHAR(255) NULL,
  parent_id BIGINT REFERENCES "Organization"(id) ON DELETE CASCADE
);